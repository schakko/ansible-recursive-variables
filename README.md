# Execute

`ansible-playbook -i inventory/ test.yml`



## Config

``` directories:
  - name: Name of the element
    type: folder / file
    permissions:
      - etype: group / user / other
        entity: users
        permission: rwx
```

